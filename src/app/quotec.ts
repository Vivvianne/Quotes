export class Quotec {
    public id:number; public authorname:string; public submitter: string;
    constructor(id:number, authorname:string, submitter: string, time:string, upvote:number, downvote:number){

    }
}
